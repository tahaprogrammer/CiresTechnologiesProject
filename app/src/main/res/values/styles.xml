<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="CustomTabLayout" parent="Base.Widget.Design.TabLayout">

        <item name="tabIndicatorColor">@android:color/black</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabPadding">4dp</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabTextAppearance">@style/MainTabTextStyle</item>
        <item name="tabSelectedTextColor">@android:color/black</item>

    </style>

    <style name="MainTabTextStyle" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:fontFamily">@font/font</item>
        <item name="textAllCaps">false</item>
        <item name="titleMargin">8dp</item>
    </style>

</resources>
